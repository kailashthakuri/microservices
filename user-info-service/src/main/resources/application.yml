eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    registry-fetch-interval-seconds: 5
spring:
  datasource:
    username: root
    password:
    url: jdbc:mysql://localhost:3306/user_info?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: user-info-service
  jpa:
    hibernate:
      ddl-auto: update